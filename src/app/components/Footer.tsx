export default function Footer() {
    return (
        <section className="fixed bottom-0 right-0 left-0 flex items-center justify-center bg-white">
            This is footer
        </section>
    );
}